{
    "name": "aws-serverless-tsc-integration",
    "version": "1.0.0",
    "description": "AWS TypeScript Integration with Terraform",
    "main": "dist/index.js",
    "scripts": {
        "build": "tsc",
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "devDependencies": {
        "@types/aws-lambda": "^8.10.140",
        "@types/node": "^20.14.9",
        "typescript": "^5.5.2"
    },
    "dependencies": {
        "@aws-sdk/client-s3": "^3.600.0",
        "aws-lambda": "^1.0.7"
    }
}
